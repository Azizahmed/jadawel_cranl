# Copyright Jadawel B.V. All Rights Reserved.

## @section Global parameters
## Global Docker image parameters
## Please, note that this will override the image parameters, including dependencies, configured to use the global value
## Current available global Docker image parameters, serviceAccount, backendConfigMap, backendSecret, frontendConfigMap, envFrom, domain, backendDomain.
##

## @param global.jadawel.imageRegistry Global Docker image registry
## @param global.jadawel.imagePullSecrets Global Docker registry secret names as an array
## @param global.jadawel.image.tag Global Docker image tag
## @param global.jadawel.serviceAccount.shared Set to true to share the service account between all application components.
## @param global.jadawel.serviceAccount.create Set to true to create a service account to share between all application components.
## @param global.jadawel.serviceAccount.name Configure a name for service account to share between all application components.
## @param global.jadawel.serviceAccount.annotations Configure annotations for the shared service account.
## @param global.jadawel.serviceAccount.automountServiceAccountToken Automount the service account token to the pods.
## @param global.jadawel.backendConfigMap Configure a name for the backend configmap.
## @param global.jadawel.backendSecret Configure a name for the backend secret.
## @param global.jadawel.frontendConfigMap Configure a name for the frontend configmap.
## @param global.jadawel.sharedConfigMap Configure a name for the shared configmap.
## @param global.jadawel.envFrom Configure secrets or configMaps to be used as environment variables for all components.
## @param global.jadawel.domain Configure the domain for the frontend application.
## @param global.jadawel.backendDomain Configure the domain for the backend application.
## @param global.jadawel.objectsDomain Configure the domain for the external facing minio api.
## @param global.jadawel.containerSecurityContext.enabled Enabled containers' Security Context
## @param global.jadawel.containerSecurityContext.seLinuxOptions [object,nullable] Set SELinux options in container
## @param global.jadawel.containerSecurityContext.runAsUser Set containers' Security Context runAsUser
## @param global.jadawel.containerSecurityContext.runAsGroup Set containers' Security Context runAsGroup
## @param global.jadawel.containerSecurityContext.runAsNonRoot Set container's Security Context runAsNonRoot
## @param global.jadawel.containerSecurityContext.privileged Set container's Security Context privileged
## @param global.jadawel.containerSecurityContext.readOnlyRootFilesystem Set container's Security Context readOnlyRootFilesystem
## @param global.jadawel.containerSecurityContext.allowPrivilegeEscalation Set container's Security Context allowPrivilegeEscalation
## @param global.jadawel.containerSecurityContext.capabilities.drop List of capabilities to be dropped
## @param global.jadawel.containerSecurityContext.capabilities.add List of capabilities to be added
## @param global.jadawel.containerSecurityContext.seccompProfile.type Set container's Security Context seccomp profile
## @param global.jadawel.securityContext.enabled Enable security context
## @param global.jadawel.securityContext.fsGroupChangePolicy Set filesystem group change policy
## @param global.jadawel.securityContext.sysctls Set kernel settings using the sysctl interface
## @param global.jadawel.securityContext.supplementalGroups Set filesystem extra groups
## @param global.jadawel.securityContext.fsGroup Group ID for the pod
##
global:
  jadawel:
    imageRegistry: jadawel
    image:
      tag: 2.2.2
    imagePullSecrets: []
    serviceAccount:
      shared: true
      create: true
      name: jadawel
      annotations: {}
      automountServiceAccountToken: false
    sharedConfigMap: shared-config
    backendConfigMap: backend-config
    backendSecret: backend-secret
    frontendConfigMap: frontend-config
    envFrom: []
    domain: cluster.local
    backendDomain: api.cluster.local
    objectsDomain: objects.cluster.local
    assistantLLMModel: ""

    securityContext:
      enabled: false
      fsGroupChangePolicy: "Always"
      sysctls: []
      supplementalGroups: []
      fsGroup: ""

    containerSecurityContext:
      enabled: false
      seLinuxOptions: {}
      runAsUser: ""
      runAsGroup: ""
      runAsNonRoot: ""
      privileged: false
      readOnlyRootFilesystem: false
      allowPrivilegeEscalation: false
      capabilities:
        add: []
        drop: []
      seccompProfile:
        type: ""

## @section Jadawel Configuration
## Configuration for the Jadawel application
## @param generateJwtSecret Generate a new JWT secret
generateJwtSecret: true

## @section Shared ConfigMap Configuration
## Configuration for the shared ConfigMap
## @param sharedConfigMap Additional configuration for the shared ConfigMap, key value map.
sharedConfigMap: {}

## @section Frontend ConfigMap Configuration
## Configuration for the Frontend ConfigMap
## @param frontendConfigMap.DOWNLOAD_FILE_VIA_XHR Set to "1" to download files via XHR
frontendConfigMap:
  DOWNLOAD_FILE_VIA_XHR: "1"
# frontendConfigMap:
#   ADDITIONAL_MODULES: ""
#   JADAWEL_DISABLE_PUBLIC_URL_CHECK: "yes"
#   JADAWEL_DISABLE_GOOGLE_DOCS_FILE_PREVIEW: ""
#   DOWNLOAD_FILE_VIA_XHR: "1"
#   JADAWEL_MAX_IMPORT_FILE_SIZE_MB: "512"
#   JADAWEL_MAX_SNAPSHOTS_PER_GROUP: "-1"

## @section backend Secrets Configuration
## Configuration for the backend Secrets
## @param backendSecrets Additional configuration for the backend Secrets, key value map.
backendSecrets: {}

## @section backend ConfigMap Configuration
## Configuration for the backend ConfigMap
## @param backendConfigMap.DONT_UPDATE_FORMULAS_AFTER_MIGRATION Set to "yes" to disable updating formulas after migration
## @param backendConfigMap.SYNC_TEMPLATES_ON_STARTUP Set to "false" to disable syncing templates on startup
## @param backendConfigMap.MIGRATE_ON_STARTUP Set to "false" to disable migration on startup
## @param backendConfigMap.JADAWEL_TRIGGER_SYNC_TEMPLATES_AFTER_MIGRATION Set to "true" to trigger syncing templates after migration
backendConfigMap:
  DONT_UPDATE_FORMULAS_AFTER_MIGRATION: "yes"
  SYNC_TEMPLATES_ON_STARTUP: "false"
  MIGRATE_ON_STARTUP: "false"
  JADAWEL_TRIGGER_SYNC_TEMPLATES_AFTER_MIGRATION: "true"
# backendConfigMap:
#   JADAWEL_AMOUNT_OF_GUNICORN_WORKERS: "5"
#   JADAWEL_BACKEND_LOG_LEVEL: DEBUG
#   JADAWEL_COUNT_ROWS_ENABLED: "true"
#   JADAWEL_DEFAULT_SEARCH_MODE: full-text-with-count
#   JADAWEL_ENABLE_OTEL: "false"
#   JADAWEL_EXTRA_ALLOWED_HOSTS: "*"
#   JADAWEL_FILE_UPLOAD_SIZE_LIMIT_MB: "20"
#   JADAWEL_GROUP_STORAGE_USAGE_ENABLED: "true"
#   DONT_UPDATE_FORMULAS_AFTER_MIGRATION: "yes"
#   ENABLE_SECURE_PROXY_SSL_HEADER: "yes"
#   MAILGUN_SENDER_DOMAIN: mail.mydomain.com
#   MIGRATE_ON_STARTUP: "false"
#   SYNC_TEMPLATES_ON_STARTUP: "false"

## @section Migration Job Configuration
## Configuration for the database migration job
## This job ensures that the database schema is up-to-date before the application starts

## @param migration.enabled Enabled the migration job
## @param migration.image.repository Migration job Docker image repository
## @param migration.priorityClassName Kubernetes priority class name for the migration job
## @param migration.nodeSelector Node labels for pod assignment
## @param migration.tolerations Tolerations for pod assignment
## @param migration.affinity Affinity settings for pod assignment
## @param migration.extraEnv Extra environment variables for the migration job
## @param migration.envFrom ConfigMaps or Secrets to be used as environment variables
## @param migration.volumes Volumes for the migration job
## @param migration.volumeMounts Volume mounts for the migration job
## @param migration.securityContext.enabled Enable security context
## @param migration.securityContext.fsGroupChangePolicy Set filesystem group change policy
## @param migration.securityContext.sysctls Set kernel settings using the sysctl interface
## @param migration.securityContext.supplementalGroups Set filesystem extra groups
## @param migration.securityContext.fsGroup Group ID for the pod
## @param migration.containerSecurityContext.enabled Enabled containers' Security Context
## @param migration.containerSecurityContext.seLinuxOptions [object,nullable] Set SELinux options in container
## @param migration.containerSecurityContext.runAsUser Set containers' Security Context runAsUser
## @param migration.containerSecurityContext.runAsGroup Set containers' Security Context runAsGroup
## @param migration.containerSecurityContext.runAsNonRoot Set container's Security Context runAsNonRoot
## @param migration.containerSecurityContext.privileged Set container's Security Context privileged
## @param migration.containerSecurityContext.readOnlyRootFilesystem Set container's Security Context readOnlyRootFilesystem
## @param migration.containerSecurityContext.allowPrivilegeEscalation Set container's Security Context allowPrivilegeEscalation
## @param migration.containerSecurityContext.capabilities.drop List of capabilities to be dropped
## @param migration.containerSecurityContext.capabilities.add List of capabilities to be added
## @param migration.containerSecurityContext.seccompProfile.type Set container's Security Context seccomp profile

migration:
  enabled: true
  image:
    repository: backend
  priorityClassName: ""
  nodeSelector: {}
  tolerations: []
  affinity: []
  extraEnv: []
  envFrom: []
  volumes: []
  volumeMounts: []
  securityContext:
    enabled: false
    fsGroupChangePolicy: ""
    sysctls: ""
    supplementalGroups: ""
    fsGroup: ""
  containerSecurityContext:
    enabled: false
    seLinuxOptions: {}
    runAsUser: ""
    runAsGroup: ""
    runAsNonRoot: ""
    privileged: false
    readOnlyRootFilesystem: false
    allowPrivilegeEscalation: false
    capabilities:
      add: []
      drop: []
    seccompProfile:
      type: ""

## @section Jadawel Backend ASGI Configuration
## Configuration for the ASGI server that serves the Jadawel backend application.
## This section includes the Docker image repository and arguments for running the ASGI server,
## as well as liveness and readiness probes to ensure the service is operating correctly.

## Docker Image Configuration
## @param jadawel-backend-asgi.image.repository Docker image repository for the ASGI server.
## This specifies the location of the Docker image used to run the Jadawel backend ASGI server.

## ASGI Server Arguments
## @param jadawel-backend-asgi.args Arguments passed to the ASGI server.
## This typically includes the command to start the server, such as "gunicorn" for a Gunicorn-based ASGI server.

## Liveness Probe Configuration
## Ensures the ASGI server is alive and can handle requests. If the liveness probe fails, Kubernetes restarts the container.
## @param jadawel-backend-asgi.livenessProbe.exec.command The command used to check the liveness of the ASGI server.
## @param jadawel-backend-asgi.livenessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-backend-asgi.livenessProbe.initialDelaySeconds Delay before the liveness probe is initiated after the container starts.
## @param jadawel-backend-asgi.livenessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-backend-asgi.livenessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-backend-asgi.livenessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Readiness Probe Configuration
## Ensures the ASGI server is alive and can handle requests. If the readiness probe fails, new deployments are halted.
## @param jadawel-backend-asgi.readinessProbe.exec.command The command used to check the readiness of the ASGI server.
## @param jadawel-backend-asgi.readinessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-backend-asgi.readinessProbe.initialDelaySeconds Delay before the readiness probe is initiated after the container starts.
## @param jadawel-backend-asgi.readinessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-backend-asgi.readinessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-backend-asgi.readinessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Autoscaling Configuration
## Automatically adjust the number of replicas based on CPU and memory utilization.
## @param jadawel-backend-asgi.autoscaling.enabled Enable autoscaling
## @param jadawel-backend-asgi.autoscaling.minReplicas Minimum number of replicas
## @param jadawel-backend-asgi.autoscaling.maxReplicas Maximum number of replicas
## @param jadawel-backend-asgi.autoscaling.targetCPUUtilizationPercentage Target CPU utilization percentage for autoscaling
## @param jadawel-backend-asgi.autoscaling.targetMemoryUtilizationPercentage Target memory utilization percentage for autoscaling
jadawel-backend-asgi:
  image:
    repository: backend
  args:
    - "gunicorn"
  livenessProbe:
    exec:
      command:
        - /bin/bash
        - -c
        - /jadawel/backend/docker/docker-entrypoint.sh backend-healthcheck
    failureThreshold: 3
    initialDelaySeconds: 120
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 5

  readinessProbe:
    exec:
      command:
        - /bin/bash
        - -c
        - /jadawel/backend/docker/docker-entrypoint.sh backend-healthcheck
    failureThreshold: 3
    initialDelaySeconds: 120
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 5

  autoscaling:
    enabled: false
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80

## @section Jadawel Backend WSGI Configuration
## Configuration for the WSGI server that serves the Jadawel backend application.
## This section includes the Docker image repository and arguments for running the WSGI server,
## as well as liveness and readiness probes to ensure the service is operating correctly.

## Docker Image Configuration
## @param jadawel-backend-wsgi.image.repository Docker image repository for the WSGI server.
## This specifies the location of the Docker image used to run the Jadawel backend WSGI server.

## WSGI Server Arguments
## @param jadawel-backend-wsgi.args Arguments passed to the WSGI server.
## This typically includes the command to start the server, such as "gunicorn" for a Gunicorn-based WSGI server.

## Liveness Probe Configuration
## Ensures the WSGI server is alive and can handle requests. If the liveness probe fails, Kubernetes restarts the container.
## @param jadawel-backend-wsgi.livenessProbe.exec.command The command used to check the liveness of the WSGI server.
## @param jadawel-backend-wsgi.livenessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-backend-wsgi.livenessProbe.initialDelaySeconds Delay before the liveness probe is initiated after the container starts.
## @param jadawel-backend-wsgi.livenessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-backend-wsgi.livenessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-backend-wsgi.livenessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Readiness Probe Configuration
## Ensures the wsgi server is alive and can handle requests. If the readiness probe fails, new deployments are halted.
## @param jadawel-backend-wsgi.readinessProbe.exec.command The command used to check the readiness of the wsgi server.
## @param jadawel-backend-wsgi.readinessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-backend-wsgi.readinessProbe.initialDelaySeconds Delay before the readiness probe is initiated after the container starts.
## @param jadawel-backend-wsgi.readinessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-backend-wsgi.readinessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-backend-wsgi.readinessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Autoscaling Configuration
## Automatically adjust the number of replicas based on CPU and memory utilization.
## @param jadawel-backend-wsgi.autoscaling.enabled Enable autoscaling
## @param jadawel-backend-wsgi.autoscaling.minReplicas Minimum number of replicas
## @param jadawel-backend-wsgi.autoscaling.maxReplicas Maximum number of replicas
## @param jadawel-backend-wsgi.autoscaling.targetCPUUtilizationPercentage Target CPU utilization percentage for autoscaling
## @param jadawel-backend-wsgi.autoscaling.targetMemoryUtilizationPercentage Target memory utilization percentage for autoscaling

jadawel-backend-wsgi:
  image:
    repository: backend
  args:
    - gunicorn-wsgi
    - "--timeout"
    - "120"
  livenessProbe:
    exec:
      command:
        - /bin/bash
        - -c
        - /jadawel/backend/docker/docker-entrypoint.sh backend-healthcheck
    failureThreshold: 3
    initialDelaySeconds: 120
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 5

  readinessProbe:
    exec:
      command:
        - /bin/bash
        - -c
        - /jadawel/backend/docker/docker-entrypoint.sh backend-healthcheck
    failureThreshold: 3
    initialDelaySeconds: 120
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 5

  autoscaling:
    enabled: false
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
## @section Jadawel Web Frontend Configuration
## Configuration for the Web Frontend server that serves the Jadawel backend application.
## This section includes the Docker image repository and arguments for running the Web Frontend server,
## as well as liveness and readiness probes to ensure the service is operating correctly.

## Docker Image Configuration
## @param jadawel-frontend.image.repository Docker image repository for the Web Frontend server.
## This specifies the location of the Docker image used to run the Jadawel Web Frontend server.

## Web Frontend Server Arguments
## @param jadawel-frontend.args Arguments passed to the Web Frontend server.
## This typically includes the command to start the server, such as "nuxt".

## Web Frontend Server Working Directory
## @param jadawel-frontend.workingDir Working Directory for the container.
## This should be empty for the Web Frontend Server.

## Liveness Probe Configuration
## Ensures the Web Frontend server is alive and can handle requests. If the liveness probe fails, Kubernetes restarts the container.
## @param jadawel-frontend.livenessProbe.httpGet.path The path to check for the liveness probe.
## @param jadawel-frontend.livenessProbe.httpGet.port The port to check for the liveness probe.
## @param jadawel-frontend.livenessProbe.httpGet.scheme The scheme to use for the liveness probe.
## @param jadawel-frontend.livenessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-frontend.livenessProbe.initialDelaySeconds Delay before the liveness probe is initiated after the container starts.
## @param jadawel-frontend.livenessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-frontend.livenessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-frontend.livenessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Readiness Probe Configuration
## Ensures the wsgi server is alive and can handle requests. If the readiness probe fails, new deployments are halted.
## @param jadawel-frontend.readinessProbe.httpGet.path The path to check for the readiness probe.
## @param jadawel-frontend.readinessProbe.httpGet.port The port to check for the readiness probe.
## @param jadawel-frontend.readinessProbe.httpGet.scheme The scheme to use for the readiness probe.
## @param jadawel-frontend.readinessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-frontend.readinessProbe.initialDelaySeconds Delay before the readiness probe is initiated after the container starts.
## @param jadawel-frontend.readinessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-frontend.readinessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-frontend.readinessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Mount Configuration Configuration
## Determines which configurations the Web Frontend server should mount.
## @param jadawel-frontend.mountConfiguration.backend If enabled, all the backend service configurations and secrets will be mounted.
## @param jadawel-frontend.mountConfiguration.frontend If enabled, all the frontend service configurations and secrets will be mounted.
## @param jadawel-frontend.service.targetPort The port the Web Frontend server listens on.

## Autoscaling Configuration
## Automatically adjust the number of replicas based on CPU and memory utilization.
## @param jadawel-frontend.autoscaling.enabled Enable autoscaling
## @param jadawel-frontend.autoscaling.minReplicas Minimum number of replicas
## @param jadawel-frontend.autoscaling.maxReplicas Maximum number of replicas
## @param jadawel-frontend.autoscaling.targetCPUUtilizationPercentage Target CPU utilization percentage for autoscaling
## @param jadawel-frontend.autoscaling.targetMemoryUtilizationPercentage Target memory utilization percentage for autoscaling

jadawel-frontend:
  image:
    repository: web-frontend
  workingDir: ""
  args:
    - nuxt
  mountConfiguration:
    backend: false
    frontend: true
  service:
    targetPort: 3000

  readinessProbe:
    failureThreshold: 3
    httpGet:
      path: /_health
      port: 3000
      scheme: HTTP
    initialDelaySeconds: 5
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 5
  livenessProbe:
    failureThreshold: 3
    httpGet:
      path: /_health
      port: 3000
      scheme: HTTP
    initialDelaySeconds: 5
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 5

  autoscaling:
    enabled: false
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80

## @section Jadawel Celery beat Configuration
## Configuration for the Celery beat workers that process background tasks for the Jadawel application.
## This section includes the Docker image repository and arguments for running the Celery beat workers,
## @param jadawel-celery-beat-worker.image.repository Docker image repository for the Celery beat worker.
## @param jadawel-celery-beat-worker.args Arguments passed to the Celery beat worker.
## @param jadawel-celery-beat-worker.replicaCount Number of replicas for the Celery beat worker.
## @param jadawel-celery-beat-worker.service.create Set to false to disable creating a service for the Celery beat worker.
jadawel-celery-beat-worker:
  image:
    repository: backend
  args:
    - celery-beat
  replicaCount: 1
  service:
    create: false

## @section Jadawel Celery export worker Configuration
## Configuration for the Celery export worker that process background tasks for the Jadawel application.
## This section includes the Docker image repository and arguments for running the Celery export worker,
## @param jadawel-celery-export-worker.image.repository Docker image repository for the Celery export worker.
## @param jadawel-celery-export-worker.args Arguments passed to the Celery export worker.
## @param jadawel-celery-export-worker.replicaCount Number of replicas for the Celery export worker.
## @param jadawel-celery-export-worker.service.create Set to false to disable creating a service for the Celery beat worker.
jadawel-celery-export-worker:
  image:
    repository: backend
  args:
    - celery-exportworker
  replicaCount: 1
  service:
    create: false

## @section Jadawel Celery worker Configuration
## Configuration for the Celery worker that process background tasks for the Jadawel application.
## This section includes the Docker image repository and arguments for running the Celery worker,
## @param jadawel-celery-worker.image.repository Docker image repository for the Celery worker.
## @param jadawel-celery-worker.args Arguments passed to the Celery worker.
## @param jadawel-celery-worker.replicaCount Number of replicas for the Celery worker.
## @param jadawel-celery-worker.service.create Set to false to disable creating a service for the Celery beat worker.
## Liveness Probe Configuration
## Ensures the Celery worker is alive and can handle requests. If the liveness probe fails, Kubernetes restarts the container.
## @param jadawel-celery-worker.livenessProbe.exec.command The command used to check the liveness of the WSGI server.
## @param jadawel-celery-worker.livenessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-celery-worker.livenessProbe.initialDelaySeconds Delay before the liveness probe is initiated after the container starts.
## @param jadawel-celery-worker.livenessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-celery-worker.livenessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-celery-worker.livenessProbe.timeoutSeconds Number of seconds after which the probe times out.

## Readiness Probe Configuration
## Ensures the wsgi server is alive and can handle requests. If the readiness probe fails, new deployments are halted.
## @param jadawel-celery-worker.readinessProbe.exec.command The command used to check the readiness of the wsgi server.
## @param jadawel-celery-worker.readinessProbe.failureThreshold Number of times the probe can fail before the container is restarted.
## @param jadawel-celery-worker.readinessProbe.initialDelaySeconds Delay before the readiness probe is initiated after the container starts.
## @param jadawel-celery-worker.readinessProbe.periodSeconds How often (in seconds) to perform the probe.
## @param jadawel-celery-worker.readinessProbe.successThreshold Minimum consecutive successes for the probe to be considered successful after having failed.
## @param jadawel-celery-worker.readinessProbe.timeoutSeconds Number of seconds after which the probe times out.
jadawel-celery-worker:
  image:
    repository: backend
  args:
    - celery-worker
  replicaCount: 1
  service:
    create: false
  livenessProbe:
    exec:
      command:
        - /bin/bash
        - -c
        - /jadawel/backend/docker/docker-entrypoint.sh celery-worker-healthcheck
    failureThreshold: 3
    initialDelaySeconds: 10
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 10

  readinessProbe:
    exec:
      command:
        - /bin/bash
        - -c
        - /jadawel/backend/docker/docker-entrypoint.sh celery-worker-healthcheck
    failureThreshold: 3
    initialDelaySeconds: 10
    periodSeconds: 30
    successThreshold: 1
    timeoutSeconds: 10

## @section Jadawel Celery Flower Configuration
## Configuration for the Celery Flower monitoring tool that provides a web-based interface for monitoring Celery workers.
## @param jadawel-celery-flower.enabled Set to true to enable the Celery Flower monitoring tool.
## @param jadawel-celery-flower.image.repository Docker image repository for the Celery Flower monitoring tool.
## @param jadawel-celery-flower.args Arguments passed to the Celery Flower monitoring tool.
## @param jadawel-celery-flower.replicaCount Number of replicas for the Celery Flower monitoring tool.
jadawel-celery-flower:
  enabled: false
  image:
    repository: backend
  args:
    - celery-flower
  replicaCount: 1

## Configuration for the Jadawel Embeddings service
## @param jadawel-embeddings.enabled Set to true to enable the Jadawel Embeddings service.
## @param jadawel-embeddings.assistantLLMModel The LLM model to use for the AI assistant (e.g., "groq/openai/gpt-oss-120b").
## @param jadawel-embeddings.image.repository Docker image repository for the Embeddings service.
## @param jadawel-embeddings.resources Resource requests and limits for the Embeddings service.
## @param jadawel-embeddings.autoscaling.enabled Enable autoscaling for the Embeddings service.
## @param jadawel-embeddings.autoscaling.minReplicas Minimum number of replicas for autoscaling.
## @param jadawel-embeddings.autoscaling.maxReplicas Maximum number of replicas for autoscaling.
## @param jadawel-embeddings.autoscaling.targetCPUUtilizationPercentage Target CPU utilization percentage for autoscaling.
## @param jadawel-embeddings.service.port Service port for the Embeddings service.
## @param jadawel-embeddings.service.targetPort Target port for the Embeddings service.
## @param jadawel-embeddings.readinessProbe.initialDelaySeconds Initial delay for readiness probe.
## @param jadawel-embeddings.readinessProbe.periodSeconds Period for readiness probe.
## @param jadawel-embeddings.readinessProbe.timeoutSeconds Timeout for readiness probe.
## @param jadawel-embeddings.livenessProbe.initialDelaySeconds Initial delay for liveness probe.
## @param jadawel-embeddings.livenessProbe.periodSeconds Period for liveness probe.
## @param jadawel-embeddings.livenessProbe.timeoutSeconds Timeout for liveness probe.
jadawel-embeddings:
  enabled: false
  image:
    repository: embeddings
  resources:
    requests:
      cpu: 800m
      memory: 1536Mi
    limits:
      cpu: 1000m
      memory: 1536Mi
  readinessProbe:
    enabled: true
    httpGet:
      path: /health
      port: 80
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  livenessProbe:
    enabled: true
    httpGet:
      path: /health
      port: 80
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 3
    targetCPUUtilizationPercentage: 80
  service:
    type: ClusterIP
    port: 80
    targetPort: 80

## @section Ingress Configuration
## Configuration for the Ingress resource
## @param ingress.enabled Enable the Ingress resource
## @param ingress.annotations.kubernetes.io/ingress.class Ingress class annotation
## @param ingress.tls TLS configuration for the Ingress resource
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: caddy
  tls: []

## @section Redis Configuration
## Configuration for the Redis database
## @param redis.enabled Enable the Redis database
## @param redis.architecture The Redis architecture
## @param redis.auth.enabled Enable Redis authentication
## @param redis.auth.password The password for the Redis database
## @param redis.auth.existingSecret The name of an existing secret containing the database password
redis:
  image:
    registry: docker.io
    repository: bitnamilegacy/redis
  enabled: true
  architecture: standalone
  auth:
    enabled: true
    existingSecret: ""
    password: jadawel

## @section PostgreSQL Configuration
## Configuration for the PostgreSQL database
## @param postgresql.enabled Enable the PostgreSQL database
## @param postgresql.auth.database The name of the database
## @param postgresql.auth.existingSecret The name of an existing secret containing the database password
## @param postgresql.auth.password The password for the database
## @param postgresql.auth.username The username for the database
postgresql:
  image:
    registry: docker.io
    repository: bitnamilegacy/postgresql
  enabled: true
  auth:
    database: jadawel
    existingSecret: ""
    password: jadawel
    username: jadawel

## @section Minio Configuration
## Configuration for the Minio object storage service
## @param minio.enabled Enable the Minio object storage service
## @param minio.networkPolicy.enabled Enable the Minio network policy
## @param minio.disableWebUI Disable the Minio web UI
## @param minio.provisioning.enabled Enable the Minio provisioning service
## @param minio.provisioning.buckets[0].name Name of the bucket to create
## @param minio.provisioning.extraCommands List of extra commands to run after provisioning
minio:
  image:
    registry: docker.io
    repository: bitnamilegacy/minio
  enabled: true
  networkPolicy:
    enabled: false
  disableWebUI: true

  provisioning:
    enabled: true
    buckets:
      - name: jadawel
    extraCommands: mc anonymous set download provisioning/jadawel

## @section Caddy Configuration
## Configuration for the Caddy ingress controller
## @param caddy.enabled Enable the Caddy ingress controller
## @param caddy.ingressController.className Ingress class name which caddy will look for on ingress annotations.
## @param caddy.ingressController.config.email Email address to use for Let's Encrypt certificates
## @param caddy.ingressController.config.proxyProtocol Enable the PROXY protocol
## @param caddy.ingressController.config.experimentalSmartSort Enable experimental smart sorting
## @param caddy.ingressController.config.onDemandTLS Enable on-demand TLS
## @param caddy.ingressController.config.onDemandAsk URL to check for on-demand TLS
## @param caddy.loadBalancer.externalTrafficPolicy External traffic policy for the load balancer
## @param caddy.loadBalancer.annotations Annotations for the load balancer
caddy:
  enabled: true
  ingressController:
    className: caddy
    config:
      email: "my@email.com"
      proxyProtocol: true
      experimentalSmartSort: false
      onDemandTLS: true
      onDemandAsk: http://:9765/healthz
  loadBalancer:
    externalTrafficPolicy: "Local"
    annotations: {}
