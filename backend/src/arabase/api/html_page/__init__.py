"""API for the HTML page view type.

Mounted under ``/api/database/views/html-page/`` automatically:
``jadawel.contrib.database.api.views.urls`` builds its urlpatterns from
``view_type_registry.api_urls``, so registering the view type is enough and no
core urlconf is edited.
"""
