"""Jadawel fork version, tracked independently of the upstream Baserow version."""

VERSION = "0.1.0-dev"

# The upstream Baserow release this fork is currently based on. Update this when
# you rebase/merge the ``upstream`` remote to a newer tag.
BASEROW_BASE_VERSION = "2.2.2"
