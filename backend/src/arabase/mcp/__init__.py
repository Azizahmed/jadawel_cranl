"""MCP tools the fork adds to Jadawel's server.

Registered from :meth:`arabase.apps.ArabaseConfig.ready` into the same
``mcp_tool_registry`` core uses, so they appear alongside the built-in table and
row tools with no core edit.
"""
