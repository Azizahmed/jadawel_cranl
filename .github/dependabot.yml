# Intentionally empty in this repository.
#
# jadawel_cranl is a deployment repository: it carries the root Dockerfile and
# the image-publishing workflow on top of a copy of `Azizahmed/Jadawel`.
# Dependency updates belong in that repository, where they are reviewed and
# tested. Opening the same PRs here would produce changes that never get merged
# back, silently diverging the two trees.
version: 2
updates: []
